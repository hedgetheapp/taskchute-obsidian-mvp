# v0.6.68 BRAT Prerelease

Status: immutable BRAT test distribution. Tag target: `2c33fdaaf5f990d0045120502094934eb195bb20`. Integrated: Yes. Prereleased / Test-distributed: Yes. Verified: No. Released: No. Real Vault / remote / mobile verification is `NOT_VERIFIED`.

## Problem

Published v0.6.67 still failed `UNDO-BRIDGE-CROSS-SECTION-01`. T-0650 / E-20260816-0026 synchronized the forward afternoon-to-morning TaskMoved as D1 sequence 2392, but Ctrl+Z restored only dev Markdown. No inverse TaskMoved was created and remote/mobile stayed in morning.

The v0.6.67 capture-phase keyboard handler checked the generic editable target guard before recognizing Undo / Redo. Because that guard includes buttons, TaskBoard control focus could return before `preventDefault()` and allow Obsidian local Undo to run without the TaskChute semantic route.

## Fix

- Recognize Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z before the generic editable/button guard.
- Use one capture-phase routing gateway for TaskBoard non-text contexts.
- Consume an owned shortcut synchronously and invoke TaskChute Undo / Redo exactly once.
- Pass text inputs, textareas, selects, contenteditable elements, editors, and external overlays through to native behavior.
- Consume and block the shortcut while the TaskMoved semantic lifecycle is active so native Undo cannot mutate Markdown locally.
- Route command-palette TaskChute Undo / Redo through the same invocation gateway.
- Record bounded routing diagnostics without Markdown bodies or secrets.

## Preserved Scope

The v0.6.67 operation ID / batch ID lifecycle, exact semantic attachment, physical verification, rollback, Bridge-state preservation, inverse TaskMoved, and exact unsent net-zero rules remain unchanged. Supported cross-device Undo / Redo remains same-date single-task D&D only.

## Verification

- `node --check .\main.js`: PASS
- focused Ctrl+Z / Redo keyboard routing: PASS
- v0.6.67 semantic lifecycle regression: PASS
- v0.6.66 Undo / Redo TaskMoved regression: PASS
- TMV4, interrupt continuation, rename handoff, and insert-below regressions: PASS
- real Vault / remote / mobile: `NOT_VERIFIED`

Published v0.6.66 and v0.6.67 tags, Releases, and assets remain immutable.

## Post-publication Device Evidence

`UNDO-BRIDGE-CROSS-SECTION-01` is `FAIL` on the immutable v0.6.68 assets.

- Fixture: T-0651 / E-20260816-0027.
- Forward afternoon-to-morning TaskMoved: D1 sequence 2396, event `3a69bd85-bdc3-4858-8ddc-35245b1beb17`, remote/mobile applied.
- Ctrl+Z was routed through the TaskChute-owned gateway, but the top Undo action had no TaskMoved semantic.
- Dev returned to afternoon, inverse TaskMoved count was zero, and remote/mobile remained in morning.

This evidence does not change the published tag, Release, or assets. The fix is assigned to v0.6.69.
