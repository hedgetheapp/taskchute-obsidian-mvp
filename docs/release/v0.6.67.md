# v0.6.67 BRAT Prerelease

Status: Prereleased / Test-distributed. Integrated: Yes. Verified: No. Released: No. `UNDO-BRIDGE-CROSS-SECTION-01` real-device result is `FAIL`; plugin full-matrix verification remains `NOT_VERIFIED`.

## Problem

The published v0.6.66 forward cross-section D&D synchronized successfully, but Ctrl+Z consumed a snapshot without `bridgeTaskMovedSemantic`. Dev returned to the source section while remote / mobile remained at the forward destination, and no inverse TaskMoved was created.

Evidence: T-0648 / E-20260816-0025, forward afternoon to morning, D1 sequence 2386, event `c2863685-ada9-4fb8-8090-7c0661e16741`. No `task-undo-confirmed-markdown-v4` or `net_zero=true` was observed; the Redo action after Undo had `hasSemantic:false`.

## Candidate Fix

- Create an operation-scoped Undo batch before the first supported D&D write.
- Bind file and plugin-data capture to the same operation ID.
- Refuse timer, history-view, unrelated capture, and normal commit paths while TaskMoved semantic is pending.
- Attach semantic only to the exact operation / batch after validating task / entry identity, before / after physical order, and a stable fingerprint.
- Permit only the exact D&D path to commit after semantic attachment.
- Invalidate only the matching D&D batch on failure; preserve unrelated Undo history and the already synchronized forward state.
- Block Undo / Redo while the D&D semantic lifecycle is still open.

## Supported Scope

The scope remains same-date, single-task cross-section and same-section D&D TaskMoved Undo / Redo. TaskCreated / TaskDeleted inversion, lifecycle inversion, Routine definition inversion, date-move rekey inversion, multi-select D&D, and arbitrary snapshot-diff synchronization remain out of scope.

## Verification

- `node --check .\main.js`: PASS
- operation-scoped semantic lifecycle focused synthetic: PASS
- existing Undo / Redo TaskMoved focused synthetic: PASS
- existing TMV4, interrupt continuation, rename handoff, and insert-below focused tests: PASS
- real Vault / remote / mobile `UNDO-BRIDGE-CROSS-SECTION-01`: `FAIL`

Post-prerelease evidence: T-0650 / E-20260816-0026 moved from afternoon to morning. D1 sequence 2392 / event `806ddfc4-fc55-47fa-b3dd-0d5ea1f1d676` was applied by remote and mobile. Ctrl+Z restored dev physical Markdown to afternoon, while remote/mobile stayed in morning and no inverse TaskMoved was created. The distributed v0.6.67 hash and loaded lifecycle functions were confirmed; the group D&D route was excluded.

Published v0.6.66 tag, Release, and assets remain immutable.
