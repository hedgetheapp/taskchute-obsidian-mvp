# v0.6.70 BRAT Prerelease

Status: immutable BRAT test distribution. Tag target: `26ae1c2ff4efb5a4d07c6cb553234b7bf506cdfe`. Integrated: Yes. Prereleased / Test-distributed: Yes. Verified: No. Released: No. Targeted TaskBoard D&D Undo / Redo device evidence is PASS; plugin overall / full matrix is `NOT_VERIFIED`.

## Problem

The immutable v0.6.69 BRAT build failed `UNDO-BRIDGE-CROSS-SECTION-01` before Ctrl+Z. The actual TaskBoard D&D left no active operation, pending batch, semantic history action, or lifecycle diagnostic. Source tracing showed that row targets used `moveTaskByDrag()`, while section-container and empty-section targets used the legacy `moveTaskToSectionByDrag()` path, which could persist Markdown and enqueue TaskMoved without entering the operation-scoped Undo lifecycle.

## Fix

- Route actual row, section-container, empty-section, and mobile quick drops through one TaskBoard dispatch gateway.
- Record bounded route diagnostics before lifecycle dispatch.
- Start the section-target operation and batch before its first persistent write.
- Reuse one semantic handoff finalizer for row and section single-task routes.
- Require exact semantic attachment, commit, fingerprint, and history-top verification before normal success.
- Preserve forward synchronization and install the existing Undo barrier if semantic handoff fails after the move becomes irreversible.
- Keep group D&D explicitly classified as a separate existing route.

## Verification

- actual TaskBoard section callback semantic lifecycle: synthetic PASS
- actual TaskBoard row callback semantic lifecycle: synthetic PASS
- entry-like `selectedTaskId`, single selection, and exactly one forward TaskMoved: synthetic PASS
- actual UI callback failure barrier: synthetic PASS
- direct D&D handoff, shortcut routing, operation lifecycle, Undo / Redo, TMV4, interrupt continuation, rename, and insert-below regressions: PASS
- targeted cross-section section / row / empty-section, same-section reorder, and multi-entry exact-entry TaskBoard D&D Undo / Redo: device `PASS`
- plugin overall / full matrix: `NOT_VERIFIED`

The v0.6.70 tag, GitHub Prerelease, and three assets are now immutable. The v0.6.69 tag, GitHub Prerelease, and assets remain unchanged. Targeted device PASS is recorded below, but overall verification is incomplete; any correction must use a later version rather than replacing v0.6.70 assets.

## Post-prerelease device evidence

- `UNDO-BRIDGE-CROSS-SECTION-SECTION-TARGET-01`: PASS. T-0653 / E-20260816-0029 used the `empty-section-body` / `moveTaskToSectionByDrag` route. Forward / Undo / Redo D1 sequences 2404 / 2405 / 2406 were applied by remote and mobile; exact semantic history top was present before Ctrl+Z and all devices converged after Undo and Redo.
- `UNDO-BRIDGE-CROSS-SECTION-ROW-TARGET-01`: PASS. T-0654 / E-20260816-0030 used the `board-resolver-row` / `moveTaskByDrag` route. Forward / Undo / Redo sequences 2409 / 2410 / 2411 were applied by remote and mobile; all devices converged after Redo.
- `UNDO-BRIDGE-EMPTY-SECTION-TARGET-01`: PASS. T-0655 / E-20260816-0031 used the `empty-section-body` route to night. Forward / Undo / Redo sequences 2414 / 2415 / 2416 were applied by remote and mobile; final physical metadata was `section=夜 section_id=night` on dev / remote and mobile UI was night.
- `UNDO / REDO-BRIDGE-SAME-SECTION-01`: PASS. In afternoon, A T-0659 / E-20260817-0007, B T-0660 / E-20260817-0008, and moved C T-0661 / E-20260817-0009 changed A/B/C to C/A/B, Undo restored A/B/C, and Redo restored C/A/B. Forward / Undo / Redo sequences 2436 / 2437 / 2438 were applied by remote and mobile; exact semantic history top was present before Ctrl+Z and all devices converged to C/A/B. Route/lifecycle diagnostics were not captured for this test and are not claimed as evidence.
- `UNDO-BRIDGE-MULTI-ENTRY-01`: PASS. Original T-0662 / E-20260817-0010 and continuation T-0662 / E-20260817-0012 shared one task ID in night; only continuation E-0012 moved around control T-0664 / E-20260817-0013. After baseline 2450, Forward / Undo / Redo sequences 2451 / 2452 / 2453 and the additional Undo / Redo confirmation cycle 2454 / 2455 all targeted E-0012 and were applied by remote and mobile. Continuation TaskMoved count was 5, original E-0010 count was 0, and all devices converged. The additional cycle is not treated as evidence of a duplicate-enqueue defect without independent proof.

These PASS results verify only the targeted cross-section routes, same-section reorder, and multi-entry exact-entry TaskBoard D&D semantic Undo / Redo behavior. v0.6.70 overall remains Verified: No and Released: No. Published assets are unchanged; any future defect is fixed in a later version.
