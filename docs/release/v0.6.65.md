# v0.6.65 BRAT Prerelease

Status: Prereleased / Test-distributed for device testing. Targeted interrupt-continuation scope Verified; plugin full matrix Not Verified.

## Problem

v0.6.64 correctly created the interrupt continuation at the final physical placement, but the earlier TaskMoved v4 retained the valid intermediate target order from before continuation creation. At send time, generic strict preflight compared that payload with the current section order including the later continuation and rejected the TaskMoved as stale. The later TaskCreated and TaskStarted therefore remained pending.

## Candidate Change

- Keep generic TaskMoved v4 exact-order validation unchanged.
- Limit projection to `taskmoved_payload_source=task-start-section-move-confirmed-markdown-v3`, then find a later, sendable `creation_source=interrupt-continuation` TaskCreated in the outbox.
- Require one exact match for logical clock, date, target section, anchor entry, continuation entry/task identity, and physical adjacency.
- Temporarily remove only that continuation entry from the current target order and compare the projected intermediate order strictly with the TaskMoved payload.
- Do not mutate `target_order_entry_ids` or refer to the future continuation from the earlier TaskMoved payload.
- Recompute and verify the projection after index rebuild before allowing send.
- Fall back to generic strict comparison for missing, ambiguous, stale, unrelated, or inconsistent candidates.

## Synthetic Verification

- valid continuation projection reproducer: PASS
- no continuation strict fallback: PASS
- unrelated extra row remains blocked: PASS
- wrong anchor / date / section: PASS
- duplicate exact continuation: PASS
- earlier TaskCreated logical clock: PASS
- wrong anchor / continuation task identity: PASS
- sent / superseded / retry-limit candidate rejection and retryable-failed recovery: PASS
- post-rebuild position / status / duplicate-candidate change: PASS
- TaskStopped -> TaskMoved -> TaskCreated -> TaskStarted order: PASS
- generic D&D / section / date move preflight unchanged: PASS
- inbound exact-anchor and Routine metadata paths unchanged: PASS
- existing interrupt continuation placement, TMV4, rename, and insert-below regressions: PASS
- `node --check .\main.js`: OK
- `git diff --check`: OK

## Post-prerelease Device Verification

- INTERRUPT-CONTINUATION-FINAL-PLACEMENT-01: `PASS`。T-0641 / E-20260816-0016、T-0642 / E-20260816-0017、continuation E-20260816-0018がafternoonで隣接。D1 seq 2358〜2361はremote / mobile applied、三端末が収束した。
- TMV4-MULTI-ENTRY-01: `PASS`。同一task_id T-0641のoriginalを維持してcontinuation E-20260816-0018だけを移動。D1 seq 2364 / event `23081df0-5c86-4e56-aa7b-86a7e1bf4bb4`がremote / mobile applied、三端末順が一致した。
- Routine occurrence interrupt continuation: `PASS`。T-0644 original E-20260816-0020とcontinuation E-20260816-0022がoccurrence key、date、generated date、scheduled time 15:10、routine sourceを保持。D1 seq 2373〜2376 applied、再評価後も2 occurrenceで第三重複なし。
- Explicit same-key / different-entry interrupt continuation: `PASS`。同じ`routine:T-0644:2026-08-16`で別entryを安全に保持した。
- v0.6.65 targeted interrupt-continuation scope: current device evidenceでVerified。
- v0.6.65 plugin / full matrix overall: `NOT_VERIFIED`。

Published v0.6.65 tag, GitHub Release, and assets remain immutable. This repository-only evidence update does not replace those assets. Published v0.6.64 artifacts also remain immutable.
