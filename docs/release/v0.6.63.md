# v0.6.63 BRAT Prerelease

v0.6.62実Vaultで確認された、reload後のmissing-metadata rowをsame-section D&Dするとphysical section contextを解決できずcommit前に停止する問題を修正する固定試験配布物です。Verified済み安定版ではありません。

## Root cause

- v0.6.62のrow normalizationはresolverへ渡されたdestination sectionを前提にしていました。
- row metadata欠落後のreloadではview/runtime taskの`section` / `sectionId`も空になり得るため、physical Markdownに正しい見出しがあってもhandoff前に`physical_section_unresolved`となる経路が残っていました。
- 画面add formが使用する`addTask()`は、v0.6.62でも日付note rowへ`estimate_min`しか保存していませんでした。`insertTaskBelowCurrent()`、section-top、copyはsection metadata保存済みですが、generic add pathは未対応でした。

## Changes

- D&D source/targetをcurrent Markdownからexact `entry_id`で一意解決します。
- source/destinationの物理見出しをsection contextの正とし、runtime section fieldsへ依存しません。
- same physical sectionを確認した後だけmissing row metadataを正規化します。
- 保存後はexact entry、`task_id`、physical section、row section、target orderを再検証してTaskMoved v4を1件enqueueします。
- generic `addTask()`の新規rowへ`section` / `section_id`を保存します。
- explicit wrong section IDと`__no_section__` conflictは補正せずblockします。
- general `markdown_section_mismatch` guard、Alt move、cross-section/date move、empty-source、Routine identity、Ack/cursor semanticsは変更しません。

## v0.6.62 evidence

- 通常C `T-0624 / E-20260815-0042`: A/B/C→C/A/B、devで`drag_drop_enqueue_succeeded`。remote / mobile / D1最終確認未完了のためPASS確定なし。
- missing metadata B `T-0623 / E-20260815-0041`: grab/drop後に元表示へ戻り、`drag_drop_section_identity_blocked`、`physical_section_unresolved`、`enqueue_attempted=false`。
- filter/search/query stateは空。試験前backup復元後もB row metadataは欠落。

## Checks

- `node --check .\main.js`: OK
- `git diff --check`: OK
- reload runtime empty / exact physical context synthetic: PASS
- missing metadata normalization / one TaskMoved v4 synthetic: PASS
- explicit wrong section ID block synthetic: PASS
- no-op event 0件 synthetic: PASS
- generic addTask section metadata synthetic: PASS
- existing TMV4 / v0.6.62 section handoff / rename / insert-below regressions: PASS

## Not verified on real devices

- reload後のB相当missing-metadata rowをD&Dしてphysical orderがcommitされること
- TaskMoved v4が1件だけD1へ到達し、remote / mobileのorderとappliedが一致すること
- generic add formで作成したrowにsection metadataが保存されること
- no-op D&Dがevent 0件であること
- explicit wrong section IDが従来どおりblockされること
- Alt+上下、cross-section/date move、empty-source、Routine occurrence回帰

## Assets

- `main.js`
- `manifest.json`
- `styles.css`

公開済みtag、GitHub Release、assetsは差し替えません。不具合が見つかった場合は次versionで修正します。
